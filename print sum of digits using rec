#include <stdio.h>
int sumofdigits(int n)
{
    if(n==0)
    return 0;
    else
    return n%10+sumofdigits(n/10);
}
int main() {
    // Write C code here
    int n;
    scanf("%d",&n);
    int k=sumofdigits(n);
    printf("%d",k);

    return 0;
}
