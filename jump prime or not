#include <stdio.h>

int main() {
    int i,n,c=0;
    scanf("%d",&n);
    for(i=2;i<=10;i++)
    {
    // Write C code here
    if(n%i==0)
    {
        c++;
       break;
    }
    }
    if(c==0)
    printf("prime num");
    else
    printf("not a prime num ");

    return 0;
}
