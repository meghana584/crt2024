#include <stdio.h>
int power(int m,int n)
{
if(n==0)
return 1;
else
return m*power(m,n-1);
}
int main() {
    // Write C code here
    int m,n;
    scanf("%d%d",&m,&n);
    int k= power(m,n);
    printf("%d",k);

    return 0;
}
