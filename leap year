#include <stdio.h>

int main() {
    int a;
    scanf("%d ",&a);
    if(a%4==0)
    {
        printf("leap year");
        
    }
    else
    {
        printf("not aleap year");
    }
    

    return 0;
}
